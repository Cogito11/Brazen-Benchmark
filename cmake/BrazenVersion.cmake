# Default version for local and development builds.
# Release workflows override this with the version tag, while rolling builds
# override it with a rolling build identifier.
set(BRAZEN_DEFAULT_VERSION "0.0.0-dev")